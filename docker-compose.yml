version: '3.8'

services:
  backend:
    build: .
    ports:
      - "4000:4000"
    environment:
      - PORT=4000

  frontend:
    build: ../trading-dashboard-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
